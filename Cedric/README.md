Controle: il gère tout le processus de la programmation, c'est comme le cerveau du processus  
Entrée: c'est l'ensemble des données que l'utilisateur envoi a l'ordinateur pour qu'il execute grace à la fonction input()  
Sortie:  c'est le resultat du processus de la programmation qui s'affiche a l'écran grace à la fonction print()   